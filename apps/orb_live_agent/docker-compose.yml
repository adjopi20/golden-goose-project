services:
  orb-live-agent:
    build:
      context: ../..
      dockerfile: apps/orb_live_agent/Dockerfile
    env_file:
      - ./profiles/avaxusdc_variant009_paper.conf
    volumes:
      - ./data:/app/apps/orb_live_agent/data
    restart: unless-stopped
